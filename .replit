# Replit configuration for VideoFlow

[run]
start = "npm install && npm run build && npm start"

[env]
PORT = "8080"
NODE_ENV = "production"
YTDLP_PATH = "yt-dlp"
FFMPEG_PATH = "ffmpeg"
CORS_ORIGIN = "*"
